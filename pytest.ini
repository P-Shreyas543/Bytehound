[pytest]
# Manual smoke scripts at the repo root (test_com7.py, test_headless.py) open
# real serial ports at import time. They are not unit tests and must not be
# auto-collected — restrict collection to the dedicated tests/ directory.
testpaths = tests
